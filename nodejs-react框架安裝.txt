# install nodejs
  yum install -y gcc-c++ make curl -sL https://rpm.nodesource.com/setup_12.x | sudo -E bash - sudo yum install -y nodejs

# install express
  npm install express -gd npm install -g express-generator

# 建立專案資料夾
  建立專案
  express npm install

# 開啟瀏覽器
  node ./bin/www & http://127.0.0.1:3000/

# 停止node環境
  ps aux | grep node kill -9 PROCESS_ID killall node

# 安裝自動更新外掛nodemon，並設為全局
  npm install --save-dev nodemon
  package.json>> "scripts": { "start": "node ./bin/www", "devstart": "nodemon ./bin/www" },
  npm run devstart

# 安裝mongo db
  https://docs.mongodb.com/manual/tutorial/install-mongodb-on-red-hat/

# 安裝mongo套件
  npm install mongoose 9.安裝npm 套件better-npm-run

# 使用 gzip/deflate 壓縮響應 npm install compression
